#!/bin/bash

LOG_FILE=/var/log/startup-script.log
npipe=/tmp/$$.tmp
trap "rm -f $npipe" EXIT
mknod $npipe p
tee <$npipe -a $LOG_FILE /dev/ttyS0 &
exec 1>&-
exec 1>$npipe
exec 2>&1

yum -y install docker
service docker start
docker run --name f5demo -p 80:80 -p 443:443 -d ${appContainerName}
